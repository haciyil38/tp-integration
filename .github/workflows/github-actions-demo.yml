name: GitHub Actions Demo

on:
  schedule:
    - cron: "* * * * *"

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: python job.py
