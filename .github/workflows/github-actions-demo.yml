name: Tests unitaires

on: [push]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: Recuperer le code
        uses: actions/checkout@v4

      - name: Lancer les tests unitaires
        run: python3 test_addition.py
